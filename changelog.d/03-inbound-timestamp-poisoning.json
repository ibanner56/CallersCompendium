{
  "id": "03-inbound-timestamp-poisoning",
  "user_visible": true,
  "app": {
    "changed": [
      "Inbound dance and program sync now uses the trusted sync envelope timestamps instead of conflicting metadata embedded in the record body."
    ]
  },
  "core": {
    "changed": [
      "Normalize inbound dance and program synchronization metadata at the storage boundary and in pending tombstone publication."
    ]
  }
}
