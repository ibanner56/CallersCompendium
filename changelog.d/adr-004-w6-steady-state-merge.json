{
  "id": "adr-004-w6-steady-state-merge",
  "user_visible": true,
  "app": {
    "changed": [
      "Device Sync now converges steady-state edits through one guarded pass, queues at most one follow-up, reuses unchanged peer manifests, settles pending work on shutdown, and pauses for confirmation instead of silently recreating a missing collection."
    ]
  },
  "core": {
    "changed": [
      "Add existence-first, classification-safe merge and transactional inbound-apply primitives for Device Sync, including tombstone-safe relation validation and dependent-row collision guards."
    ]
  }
}
