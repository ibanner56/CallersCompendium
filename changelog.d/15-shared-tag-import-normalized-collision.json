{
  "id": "15-shared-tag-import-normalized-collision",
  "user_visible": true,
  "app": {
    "fixed": [
      "Importing shared metadata no longer fails when an incoming tag or custom field matches one you already have but is written in a different Unicode form; the existing entry is reused instead."
    ]
  },
  "core": {
    "fixed": [
      "Plan shared-metadata tag and custom-field adoption on the repository's canonical natural key, resolving canonically-equivalent rows deterministically instead of colliding on the normalized unique key."
    ]
  }
}
