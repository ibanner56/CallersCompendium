{
  "id": "sync-tombstone-context-and-aliases",
  "user_visible": true,
  "app": {
    "fixed": [
      "Synced deletions are no longer applied on the strength of a companion deletion that the same pass then declines to apply, and a renamed difficulty level that was already merged away once is reconciled onto the surviving level instead of being reported as a conflict."
    ]
  },
  "core": {
    "fixed": [
      "Name inbound tombstones to the storage adapter only after every dependency group has settled, resolve canonical difficulty identities through the alias chain before adoption, and keep the choreography fingerprint free of collections it never compares."
    ]
  }
}
