{
  "id": "sync-revival-cancels-pending-deletion",
  "user_visible": true,
  "app": {
    "fixed": [
      "When another device brings back a record whose deletion this device was still holding — because something here was using it — the record now stays. It could previously disappear again on its own once the last thing using it was removed."
    ]
  },
  "core": {
    "fixed": [
      "Discard a deferred deletion when an inbound revival wins §6.4's existence comparison against it, so the deletion cannot land after the revival that outranked it."
    ]
  }
}
