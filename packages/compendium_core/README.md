# compendium_core

Pure-Dart domain core for Caller's Compendium: domain model, figure taxonomy,
dialect engine, and import parsers.

Design contracts live in [`/docs/design`](../../docs/design). This package
must not import Flutter (ADR-001) — enforced in CI.
